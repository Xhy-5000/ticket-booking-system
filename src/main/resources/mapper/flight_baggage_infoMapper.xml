<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.ticketbookingsystem.mapper.flight_baggage_infoMapper">
    <select id="findBaggageInfoByid" resultType="project.ticketbookingsystem.entity.flight_baggage_info">
        SELECT * FROM flight_baggage_info WHERE flight_id = #{flight_id}
    </select>

</mapper>